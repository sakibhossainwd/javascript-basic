var items = ['pen', 'book', 'magazine', 'calender', 'table'];

for (var i = 0; i < items.length; i++) {
    var item = items[i];
    if (item == 'calender') {
        continue;
    }
    // console.log(item);
}

var puriPrice = 10;
var puriQuantity = 5
;
console.log(puriPrice * puriQuantity);