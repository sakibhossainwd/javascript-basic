const userName = 'nameHere';
const inputName = 'namehere';

const upaarCase = userName.toLocaleUpperCase();
console.log(upaarCase);
if(userName.toUpperCase() === inputName.toUpperCase()){
    console.log('input valid');
}
else{
    console.log('input unvalid');
}